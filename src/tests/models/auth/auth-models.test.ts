
import { expect } from "chai";

describe("This test is to prove all concerns about user auth models ", () => {
  // test user object
  it("when user models is setting, this sould be certain model", () => {
    expect(4).to.equal(4);
  });
});
